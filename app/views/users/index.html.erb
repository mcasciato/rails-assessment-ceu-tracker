<div class="row">
    <div class="col-md-6">
        <ul class="list-group" id="users">
          <% @users.each do |user| %>
            <li class="list-group-item"><%= link_to user.name, user_path(user) %></li>
          <% end %>
        </ul>
    </div>
</div>
